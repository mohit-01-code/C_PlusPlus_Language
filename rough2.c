#include <stdio.h>
#define MAX_SIZE 10 //This macro [MAX_SIZE] has a value of 10
int main(void)
{
    int size = 0;
    size = size + MAX_SIZE;
    printf("\nThe value of size is [%d]\n", size);
    return 0;
}